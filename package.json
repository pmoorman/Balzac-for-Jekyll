{
  "name": "growthcasts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rollup -c & jekyll build",
    "watch": "jekyll serve --livereload & rollup -c --watch"
  }
}
